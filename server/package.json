{
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no 'test' specified\" && exit 1",
    "start": "echo \"Error: no 'start' specified\" && exit 1",
    "start:dev": "echo \"Error: no 'start:dev' specified\" && exit 1"
  }
}
